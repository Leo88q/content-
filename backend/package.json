{
  "name": "@sixsec/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SixSec: очередь модерации и API контент-завода",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "dev": "node --experimental-strip-types src/api/server.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
